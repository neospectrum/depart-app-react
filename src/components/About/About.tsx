import React from 'react';

import { Container } from '../containers/Container';

import './About.scss';

export const About = () => {
    return (
        <section className="about">
            <Container>
            </Container>
        </section>
    );
};